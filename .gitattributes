# Keep source fingerprints stable across operating systems.
*.py text eol=lf

# Preserve original data bytes and recorded artifact checksums at checkout.
examples/data/*.data -text
examples/data/*.tes -text
examples/data/*.names -text
examples/data/*.csv -text
examples/data/*.txt -text
docs/**/*.json -text
docs/**/*.jsonl -text
docs/**/*.txt -text

# Frozen protocols are hashed alongside their recorded experiment artifacts.
docs/*protocol.md text eol=lf
